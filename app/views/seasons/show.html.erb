<table class="w-full">
	<thead>
	<tr>
		<th></th>
		<th class="bg-gray-300 p-2 font-bold text-center"></th>
		<th class="bg-gray-300 p-2 font-bold text-center">G</th>
		<th class="bg-gray-300 p-2 font-bold text-center">Pts</th>
	</tr>
	</thead>
	<tbody>
	<% @current_season_table.each_with_index do |team_season, index| %>
		<tr class="<%= cycle('bg-white', 'bg-gray-100') %>">
			<td class="border border-gray-300 p-2 text-center"><%= index + 1 %></td>
			<td class="border border-gray-300 p-2 text-center"><%= link_to team_season.team.name, team_path(team_season.team) %></td>
			<td class="border border-gray-300 p-2 text-center"><%= team_season.completed_fixtures_count %>
			<td class="border border-gray-300 p-2 text-center"><%= team_season.points %></td>
		</tr>
	<% end %>
	</tbody>
</table>
