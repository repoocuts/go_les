<% team.played_home_matches.reverse.each do |match| %>
  <ul class="max-w-md space-y-1 list-none list-inside">
    <li class="list-group-item text-sm">
      <%= link_to "#{match.home_score} - #{match.away_score} #{match.away_team_name}(GW#{match.game_week})",
                  fixture_path(match) %>
    </li>
  <ul>
<% end %>
