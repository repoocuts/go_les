<section class="bg-green-50 w-full flex justify-center items-center">
	<div class="w-full max-w-screen-xl flex justify-center items-center">
		<div class="relative w-full aspect-w-16 aspect-h-9">
			<iframe class="absolute inset-0 w-1/2 h-1/2 m-auto" src="https://www.youtube.com/embed/xOVsji5dL-k" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
		</div>
	</div>
</section>

<!-- Second Section: Tables -->
<section class="bg-white w-full py-10 px-10">
	<div class="max-w-screen-xl mx-auto px-4 grid grid-cols-1 tablet:grid-cols-3 laptop:grid-cols-6 gap-4">
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
		<div class="bg-gray-100 p-4 rounded shadow">
			<%= render CurrentGameWeekFixturesComponent.with_collection(@current_game_week), cached: true %>
		</div>
	</div>
</section>

<!-- Third Section: Text -->
<section class="bg-purple-200 w-full flex justify-center items-center">
	<div class="max-w-screen-lg text-black-700/75 text-center px-4">
		<p class="leading-relaxed font-sans">
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
			This is a text section with a royal blue background. The text is centrally aligned and styled so that all the end of the lines are aligned. Adjust the content here as needed to fit your specific requirements.
		</p>
	</div>
</section>
