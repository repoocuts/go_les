<%= form_with(model: goals_scored_stat, class: "contents") do |form| %>
  <% if goals_scored_stat.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(goals_scored_stat.errors.count, "error") %> prohibited this goals_scored_stat from being saved:</h2>

      <ul>
        <% goals_scored_stat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
